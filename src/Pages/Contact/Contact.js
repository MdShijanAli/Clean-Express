import React from 'react';
import useTitle from '../../hoocks/useTitle';

const Contact = () => {
    useTitle('Contact')
    return (
        <div>
            <h1>Contact</h1>
        </div>
    );
};

export default Contact;